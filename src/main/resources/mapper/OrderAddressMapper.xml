<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.xianzhiyun.mapper.OrderAddressMapper">

    <insert id="insert" parameterType="com.example.xianzhiyun.entity.OrderAddress" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO order_address (buyer_id, name, mobile, school, region, detail, create_time)
        VALUES (#{buyerId}, #{name}, #{mobile}, #{school}, #{region}, #{detail}, NOW())
    </insert>

    <select id="selectById" parameterType="long" resultType="com.example.xianzhiyun.entity.OrderAddress">
        SELECT id, buyer_id AS buyerId, name, mobile, school, region, detail, create_time AS createTime
        FROM order_address WHERE id = #{id}
    </select>

</mapper>